name: Multi Job Workflow

on: push

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - run: echo "Build Sucessfull"

    test:
        needs: build
        runs-on: ubuntu-latest

        steps:
            - run: echo "Test completed"